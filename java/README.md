# Java JDK

Place the Java JDK archive here:
- `jdk.tar.gz` - Java Development Kit archive for offline installation

## Usage
The `install_java.sh` script will extract and install the JDK from this archive.
